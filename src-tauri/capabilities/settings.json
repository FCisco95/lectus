{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-cap",
  "description": "Capabilities for the settings window",
  "windows": ["settings"],
  "permissions": ["core:default", "shell:default", "os:default", "process:default"]
}
